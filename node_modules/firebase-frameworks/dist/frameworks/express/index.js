"use strict";
// Copyright 2022 Google LLC
// 
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
// 
//     http://www.apache.org/licenses/LICENSE-2.0
// 
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.build = void 0;
const fs_1 = require("fs");
const path_1 = require("path");
const templates_1 = require("./templates");
const utils_1 = require("../../utils");
const utils_2 = require("../../utils");
const { readFile, rm, mkdir, writeFile, copyFile } = fs_1.promises;
const build = (config, getProjectPath) => __awaiter(void 0, void 0, void 0, function* () {
    var _a, _b, _c;
    const packageJsonBuffer = yield readFile(getProjectPath('package.json'));
    const packageJson = JSON.parse(packageJsonBuffer.toString());
    if ((_a = packageJson.scripts) === null || _a === void 0 ? void 0 : _a.build) {
        // TODO add to the firebaseTools logs
        yield (0, utils_2.spawn)('npm', ['--prefix', getProjectPath(), 'run', 'build'], {}, stdoutChunk => {
            console.log(stdoutChunk.toString());
        }, errChunk => {
            console.error(errChunk.toString());
        });
    }
    const findServerRenderMethod = (method = [], entry) => __awaiter(void 0, void 0, void 0, function* () {
        if (!config.function)
            return undefined;
        const allowRecursion = !entry;
        entry || (entry = yield (() => __awaiter(void 0, void 0, void 0, function* () {
            try {
                const requiredProject = require(getProjectPath());
                if (requiredProject)
                    method = ['require'];
                return requiredProject;
            }
            catch (e) {
                const importedProject = yield Promise.resolve().then(() => __importStar(require(getProjectPath()))).catch(() => undefined);
                if (importedProject)
                    method = ['import'];
                return importedProject;
            }
        }))());
        if (!entry)
            return undefined;
        const { default: defaultExport, app, handle } = entry;
        if (typeof handle === 'function')
            return [...method, 'handle'];
        if (typeof app === 'function') {
            try {
                const express = app();
                if (typeof express.render === 'function')
                    return [...method, 'app'];
            }
            catch (e) { }
        }
        if (!allowRecursion)
            return undefined;
        if (typeof defaultExport === 'object') {
            if (typeof defaultExport.then === 'function') {
                const awaitedDefaultExport = yield defaultExport;
                return findServerRenderMethod([...method, 'default'], awaitedDefaultExport);
            }
            else {
                return findServerRenderMethod([...method, 'default'], defaultExport);
            }
        }
        return undefined;
    });
    const serverRenderMethod = yield findServerRenderMethod();
    let bootstrapScript = '';
    if (serverRenderMethod) {
        let stack = serverRenderMethod.slice();
        const entry = `./${packageJson.main || 'index.js'}`;
        if (stack.shift() === 'require') {
            bootstrapScript += `const bootstrap = Promise.resolve(require('${entry}'))`;
        }
        else {
            bootstrapScript += `const bootstrap = import('${entry}')`;
        }
        if (stack[0] === 'default') {
            stack.shift();
            bootstrapScript += '.then(({ default }) => default)';
        }
        if (stack[0] === 'app') {
            stack.shift();
            bootstrapScript += '.then(({ app }) => app())';
        }
        bootstrapScript += ';\n';
        const method = stack.shift();
        bootstrapScript += `const handle = async (req, res) => (await bootstrap)${method ? `.${method}` : ''}(req, res);`;
    }
    const deployPath = (...args) => config.dist ? (0, path_1.join)(config.dist, ...args) : getProjectPath('.deploy', ...args);
    const getHostingPath = (...args) => deployPath('hosting', ...args);
    yield rm(deployPath(), { recursive: true, force: true });
    if (serverRenderMethod) {
        yield mkdir(deployPath('functions'), { recursive: true });
    }
    yield mkdir(getHostingPath(), { recursive: true });
    if ((_b = packageJson.directories) === null || _b === void 0 ? void 0 : _b.serve) {
        yield (0, utils_2.exec)(`cp -r ${getProjectPath(packageJson.directories.serve, '*')} ${deployPath('hosting')}`);
    }
    let firebaseProjectConfig = null;
    const { project, site } = config;
    if (project && site) {
        // TODO check if firebase/auth is used
        const hasFirebaseDependency = !!((_c = packageJson.dependencies) === null || _c === void 0 ? void 0 : _c.firebase);
        if (serverRenderMethod && hasFirebaseDependency) {
            const { default: firebaseTools } = require('firebase-tools');
            const { sites } = yield firebaseTools.hosting.sites.list(Object.assign({ project }, (0, utils_2.defaultFirebaseToolsOptions)(deployPath())));
            const selectedSite = sites.find(it => (0, utils_1.shortSiteName)(it) === site);
            if (selectedSite) {
                const { appId } = selectedSite;
                if (appId) {
                    const result = yield firebaseTools.apps.sdkconfig('web', appId, (0, utils_2.defaultFirebaseToolsOptions)(deployPath()));
                    firebaseProjectConfig = result.sdkConfig;
                }
                else {
                    // TODO allow them to choose
                    console.warn(`No Firebase app associated with site ${site}, unable to provide authenticated server context`);
                }
            }
        }
    }
    if (serverRenderMethod) {
        const npmPackResults = JSON.parse(yield (0, utils_2.exec)(`npm pack ${getProjectPath()} --dry-run --json`));
        yield Promise.all(
        // TODO types
        npmPackResults.
            find(({ name }) => name === packageJson.name).
            files.
            map(({ path }) => mkdir((0, path_1.dirname)(deployPath('functions', path)), { recursive: true }).then(() => copyFile(getProjectPath(path), deployPath('functions', path)))));
        yield Promise.all([
            copyFile(getProjectPath('package-lock.json'), deployPath('functions', 'package-lock.json')).catch(() => { }),
            copyFile(getProjectPath('yarn.lock'), deployPath('functions', 'yarn.lock')).catch(() => { }),
            (0, templates_1.newPackageJson)(packageJson, getProjectPath).then(json => writeFile(deployPath('functions', 'package.json'), json)),
            writeFile(deployPath('functions', 'server.js'), (0, templates_1.newServerJs)(config, firebaseProjectConfig, bootstrapScript)),
        ]);
        // TODO add to the firebaseTools log
        yield (0, utils_2.spawn)('npm', ['i', '--prefix', deployPath('functions'), '--only', 'production'], {}, stdoutChunk => {
            console.log(stdoutChunk.toString());
        }, errChunk => {
            console.error(errChunk.toString());
        });
    }
    return { usingCloudFunctions: !!serverRenderMethod };
});
exports.build = build;
