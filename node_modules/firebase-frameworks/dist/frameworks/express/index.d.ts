import { DeployConfig, PathFactory } from '../../utils';
export declare const build: (config: DeployConfig | Required<DeployConfig>, getProjectPath: PathFactory) => Promise<{
    usingCloudFunctions: boolean;
}>;
