import type { FirebaseOptions } from 'firebase/app';
import { DeployConfig } from '../../utils';
export declare const newServerJs: (config: DeployConfig, options: FirebaseOptions | null) => string;
export declare const newPackageJson: (packageJson: any) => string;
