import type { FirebaseOptions } from 'firebase/app';
import { DeployConfig, PathFactory } from '../../utils';
export declare const newServerJs: (config: DeployConfig, options: FirebaseOptions | null, isNuxt3: boolean) => string;
export declare const newPackageJson: (packageJson: any, getProjectPath: PathFactory) => Promise<string>;
